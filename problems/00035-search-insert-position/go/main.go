package main

func searchInsert(nums []int, target int) int {
	var (
		left  int = 0
		right int = len(nums) - 1
		mid   int
	)
	for left < right {
		mid = (right - left) / 2
		if nums[mid] > 
	}
	return mid
}
